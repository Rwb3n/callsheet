// Dashboard overview — S5 §2.1, CS-WORK-043 AC-2, AC-3
// Server component placeholder — renders listing cards grid.
// Full UI implementation deferred to E2E Phase 2 (PP-Q1 component library).

export default function DashboardOverview() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Provider dashboard overview — listing cards rendered via tRPC getOverview query.</p>
    </div>
  )
}
