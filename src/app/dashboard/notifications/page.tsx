// Notification centre — S5 §6, CS-WORK-046
// Server component placeholder — full UI deferred to component library phase.

export default function NotificationCentre() {
  return (
    <div>
      <h1>Notifications</h1>
      <p>Notification centre — list, dismiss, mark-read via tRPC notification router.</p>
    </div>
  )
}
