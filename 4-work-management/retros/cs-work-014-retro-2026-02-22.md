# Retro: CS-WORK-014

**Date:** 2026-02-22
**Scope:** CS-WORK-014 — Freelancer listing creation (S2, CH-CS-004, onboarding-and-claims arc)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | How much existing infrastructure was already in place. The integrity pipeline (checkDuplicate, checkCHUniqueness, verifyIdentity), event bus, scheduler, email service, and test fixtures were all ready to wire together. The work item was mostly composition, not invention. |
| **What went well?** | Single-pass implementation — 0 type errors on first compile, all 7 tests passed on first run, all 285 tests (147 unit + 138 integration) green without regressions. The codebase patterns are mature enough that following them is mechanical. |
| **Could have gone better?** | The `createFreelancer` mutation has 5 sequential post-transaction side effects (event emit → schedule progressive disclosure → send email). This is the "God route" anti-pattern flagged in `output-style-engineer.md` §9. Each effect is a named function, but orchestration is inline in the handler. |
| **Keep doing** | Reading all related source files before writing any code. The upfront exploration (2 parallel sub-agents) surfaced the integrity pipeline in `src/domains/`, the scheduler DB adapter pattern, and the test fixture library — all critical for correct composition. |
| **Stop doing** | Nothing to stop. Session was clean. |
| **Start doing** | Consider extracting a `createListingOrchestrator(deps)` function that encapsulates the post-transaction sequence (emit → schedule → email). CS-WORK-015 (company path) and CS-WORK-016 (progressive disclosure execution) will need the same orchestration. Extract at 3 uses (rule of three). |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Existing infrastructure (bus, scheduler, integrity, email) composed cleanly | Feature | Patterns established in S0/S1 are paying dividends — new work items are composition, not construction. |
| 2 | Zero regressions on 285-test suite | Feature | Test harness and fixture conventions are stable. |
| 3 | Post-transaction orchestration is inline in the route handler | Refactor | 5 sequential effects after the DB transaction. Not broken, but will duplicate when company path lands. Extract when CS-WORK-015 arrives (rule of three). |
| 4 | `primarySectorSlug` requires a DB lookup to resolve `sectorId` | Upgrade | The caller sends `primarySectorSlug` + `primaryServiceAreaId`. The router resolves `sectorId` from the service area's FK. This works but adds a query. Could accept `sectorId` directly from the client instead. |
| 5 | Slug generation uses random suffix — no collision retry beyond one attempt | Upgrade | Astronomically unlikely to collide with 6 random chars, but a proper retry loop would be more defensive. Low priority — current approach works at CALLSHEET scale. |
| 6 | `createCompany` mutation not yet implemented | Feature request | CS-WORK-014 scope was freelancer only. Company path (with CH lookup, flagged path, `suspended`/`pending_review` defaults) is the next major addition, likely a separate work item or extension of CS-WORK-014. |

---

## 3 — Action Register

| # | Item | Priority | Owner | Definition of Done |
|---|------|----------|-------|--------------------|
| 1 | Extract post-transaction orchestration into shared function when company path lands | next | Platform & Product | `createListingOrchestrator()` used by both `createFreelancer` and `createCompany`. Route handlers call it instead of inlining 5 sequential effects. |
| 2 | Evaluate accepting `sectorId` directly from client to skip `resolveSectorId` query | later | Platform & Product | Decision documented: either remove the lookup (client sends sectorId) or keep it (server authoritative). No ambiguity about which approach is used. |
| 3 | Implement `createCompany` mutation (company path with CH lookup + flagged path) | next | Platform & Product | `createCompany` mutation exists, AC verified, company-specific integrity checks (CH uniqueness + identity verification) exercised in tests. |
