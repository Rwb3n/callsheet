---
triaged: true
status: active
---

# Retro: CS-WORK-074 — Demo preparation, S8 close-out

**Date:** 2026-03-06
**Scope:** CS-WORK-074 (12 AC). Extended demo seed with admin user, operations/commercial/buyer data across 10 tables. DEMO-SCRIPT.md with 3-journey walkthrough. Final S8 work item — S8 code complete (9/9, 93/93 AC).

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The `churn_analysis_log.accountId` column is `uuid` type (not `text` like Better Auth user.id). Required `sql\`${userId}::uuid\`` cast for the insert. This is the only table in the codebase with a soft-reference uuid `accountId` — all others use `text` FK to `user.id`. The design rationale is GDPR: the column survives account deletion for erasure flow analytics. |
| **What went well?** | All 12 ACs implemented in a single pass with zero test regressions (1440 tests). The existing demo seed's idempotency pattern (existence check → skip) scaled cleanly to 10 additional table blocks. Listing ID capture via `Map<slug, id>` during the creation loop gave clean FK references downstream without extra queries. |
| **Could have gone better?** | The local DB required `drizzle-kit push` + `db:seed` + `db:custom-sql` before the demo seed could run — the taxonomy tables were empty. This is expected (README documents the pipeline) but added ~2 minutes of setup. A `db:reset` convenience script exists in package.json but requires `npx supabase db reset` which wipes everything. |
| **Keep doing** | Running the seed twice to verify idempotency as an explicit verification step. The second run's "already exists, skipping" output is a clean AC-11 evidence trail. |
| **Stop doing** | N/A — no ceremony failures. |
| **Start doing** | N/A — demo seed is a one-off work item type. |
| **Skill amendment?** | N/A. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | `churn_analysis_log.accountId` is uuid, not text — soft reference design for GDPR | Feature | Intentional design. The `sql` cast is correct. Document the pattern for future seed scripts. |
| 2 | Listing ID capture via Map during seed loop | Feature | Clean FK reference pattern — no extra queries needed. |
| 3 | Idempotency verified by double-run | Feature | Existence-check-then-skip pattern is reliable for all 10 table blocks. |
| 4 | S8 code complete — 9/9 work items, 93/93 AC | Feature | Chapter CH-CS-010 closed. |

---

## 3 — Action Register

No actionable items — all classifications are "Feature" (protect, no work needed). S8 is complete.

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| — | No actions | — | — | — | — |
