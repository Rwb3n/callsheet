---
triaged: true
status: complete
---

# Retro: CS-WORK-013

**Date:** 2026-02-22
**Scope:** CS-WORK-013 — Account creation and onboarding router (S2, first work item)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | How clean the cut was. 5 AC, 5 deliverables, 11 tests, zero friction. No ambiguity in the spec, no hidden dependencies, no schema conflicts with existing migrations. The WORK.md decomposition precisely matched the implementation surface — nothing was missing, nothing was surplus. This is the dividend of thorough decomposition in Phase 4. |
| **What went well?** | (1) Session init + codebase exploration parallelised effectively — full picture of auth, schema, routers, test harness, and email infrastructure in one agent round-trip. (2) Migration generated and applied cleanly — `departments text[]` with default `'{}'` required zero manual SQL. (3) Test count went from 264 → 275 with zero regressions across all 13 integration test files. (4) `ensureProfile` idempotency pattern (upsert via `onConflictDoNothing`) is the right call — signup hooks may fire more than once. (5) Anonymous enquiry linking (AC-04) wired into `ensureProfile` rather than as a separate step — reduces the API surface and guarantees linking happens on first profile creation. |
| **Could have gone better?** | (1) The `assertEmailVerified` guard is a standalone function, not a tRPC middleware. The S2 spec calls it a "reusable middleware/guard" — a tRPC middleware would compose better with `protectedProcedure` for listing creation routes in CS-WORK-014. Current approach requires callers to invoke the guard manually. This is a conscious trade-off (simplicity over composability at this stage), but worth monitoring. (2) AC-02 (email verification callback page) is fully E2E — the server-side mechanism was already in place from S0's Better Auth config. No incremental server-side work was needed, which means AC-02 is effectively "already done on the server" but can't be marked complete without the E2E page test. The E2E debt list grows. |
| **Keep doing** | (1) Reading the full codebase state before writing any code — the Explore agent sweep caught the exact patterns needed (factory routers, test matrix, fixture helpers). (2) Running both `tsc --noEmit` and full test suites before declaring done. (3) Task tracking with explicit dependency chains. |
| **Stop doing** | Nothing to flag — this was a clean session. |
| **Start doing** | (1) Consider whether `assertEmailVerified` should become a tRPC middleware (`verifiedProcedure = protectedProcedure.use(verifiedEmailMiddleware)`) before CS-WORK-014 starts. If CS-WORK-014 needs it on every listing creation route, a middleware is less error-prone than manual calls. (2) Update IMPLEMENTATION-TRACKER.md after each work item completion — this session didn't update it. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | WORK.md decomposition matched implementation surface exactly | Feature | Decomposition quality is paying off — protect this by continuing the same rigour for CS-WORK-014 through CS-WORK-021. |
| 2 | Codebase exploration via sub-agent before writing code | Feature | Eliminated false starts and wrong assumptions. One round-trip captured auth config, schema, router patterns, test fixtures, email types. |
| 3 | `ensureProfile` idempotency via `onConflictDoNothing` | Feature | Correct pattern for signup hooks that may fire multiple times. |
| 4 | `assertEmailVerified` is a function, not a tRPC middleware | Refactor | Works for CS-WORK-013 (only needs to be importable). CS-WORK-014 listing creation routes may benefit from a `verifiedProcedure` middleware that composes with `protectedProcedure`. Evaluate before starting CS-WORK-014. |
| 5 | AC-02 E2E debt accumulating | Upgrade | 7 ACs now deferred to E2E (6 from S0 + AC-02). No E2E harness exists yet. Risk: E2E debt grows faster than the harness to verify it. |
| 6 | IMPLEMENTATION-TRACKER.md not updated in this session | Bug | Tracker is the authoritative record. Must be updated after every work item completion. |
| 7 | Webhook `updatedAt` timestamp flake (AC-09) | Bug | Pre-existing: 10ms sleep racing Postgres `defaultNow()`. Surfaces intermittently when DB insert and handler execute within the same millisecond. |

---

## 3 — Action Register

| # | Item | Priority | Owner | Definition of Done |
|---|------|----------|-------|--------------------|
| 1 | ~~Evaluate `assertEmailVerified` → `verifiedProcedure` middleware before CS-WORK-014~~ | ~~now~~ | ~~engineer~~ | **DONE.** Refactored: `verifiedProcedure` added to `src/server/trpc.ts` (composes `authMiddleware + verifiedEmailMiddleware`). Standalone `assertEmailVerified` kept for non-tRPC contexts. 3 new tests (FORBIDDEN unverified, pass verified, UNAUTHORIZED no session). 14 total integration tests pass. |
| 2 | ~~Update IMPLEMENTATION-TRACKER.md with CS-WORK-013 completion~~ | ~~now~~ | ~~engineer~~ | **DONE.** Tracker updated: S2 section added (1/9 work items, 4/41 + 1 e2e AC), test counts 278 (147 unit + 131 integration), E2E debt +S2-AC-02, completion log entry. |
| 3 | Plan E2E test harness before E2E debt exceeds 10 ACs | next | engineer | E2E debt section in IMPLEMENTATION-TRACKER.md has a plan reference (which work item or chapter addresses it) and a threshold trigger ("build harness when N ACs deferred"). |
| 4 | ~~Fix webhook `updatedAt` timestamp flake~~ | ~~now~~ | ~~engineer~~ | **DONE.** `webhook.integration.test.ts` AC-09: replaced 10ms sleep with backdated `updatedAt` (−60s). Deterministic — no timing dependency. |
