# Retro: CS-WORK-016

**Date:** 2026-02-22
**Scope:** CS-WORK-016 — Claim path and pre-claim snapshots (S2 §5, §7.2, §12)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The WORK.md had been rewritten since the original briefing. User said "progressive disclosure execution, 6 AC" but the actual WORK.md was "Claim path and pre-claim snapshots, 7 AC". The spec had been re-decomposed. Reading the authoritative file before starting prevented building the wrong thing. |
| **What went well?** | Clean implementation with zero type errors on first pass. All 17 integration tests passed on first run. The S2/S3 boundary was well-specified — stress test findings (S2-ST-1, S2-ST-14, S2-ST-17) resolved most ambiguities in the slice spec. The router deps were minimal (`db` + `schedulerDb` only) because the spec correctly identified that claim submission produces no side effects (no events, no emails). Existing infrastructure (`createTestListing`, `createSchedulerDb`, `ActionHandlerRegistry`) covered all test needs without new fixture code. |
| **Could have gone better?** | The WORK.md deliverables listed `src/lib/scheduler/handlers/pre-claim-snapshot-cleanup.ts` but the handler was placed at `src/lib/onboarding/pre-claim-snapshot-cleanup.ts` instead — co-located with the claim module rather than in a generic `handlers/` directory. The WORK.md path was slightly wrong (no `handlers/` dir exists yet), but this divergence should be noted. The `createTestListing` fixture required `null as unknown as string` for unclaimed listings — a type ergonomics gap that could be fixed with an overload or optional param. |
| **Keep doing** | Reading the authoritative WORK.md before starting (not relying on the user's summary). Minimal router deps — only inject what's actually used. Parallel exploration agents for spec reading + codebase scanning. Updating tracker + memory immediately after implementation. |
| **Stop doing** | Nothing identified. |
| **Start doing** | Consider adding a `createUnclaimedListing` factory to `test-fixtures.ts` — the claim-path test needed it and it'll be reused by S3 (claim evaluation, competing claims, manual review). |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | WORK.md divergence from user briefing caught by reading authoritative file | Feature | The discipline of reading WORK.md before starting prevented building the wrong work item. Protect this pattern. |
| 2 | Zero type errors + all tests passing on first run | Feature | Strong spec + existing infrastructure + stress test resolutions made implementation straightforward. |
| 3 | Minimal router deps pattern (`db` + `schedulerDb` only) | Feature | Claim submission has no side effects by design — the dep surface reflects this. Clean boundary with S3. |
| 4 | `createTestListing` requires `null as unknown as string` for unclaimed listings | Refactor | The fixture helper types `accountId` as `string` but unclaimed listings need `null`. An optional param or overload would be cleaner. |
| 5 | Handler file placed at `onboarding/` not `scheduler/handlers/` | Feature request | No `handlers/` directory exists yet. When one is created (S3+), consider whether claim-specific handlers belong there or stay co-located with their domain module. No action now — co-location is valid. |
| 6 | `createUnclaimedListing` factory missing from `test-fixtures.ts` | Feature request | S3 will need unclaimed listings for claim evaluation, competing claims, and manual review tests. Extracting the helper from claim-path tests to fixtures prevents duplication. |

---

## 3 — Action Register

| # | Item | Priority | Owner | Definition of Done |
|---|------|----------|-------|--------------------|
| 1 | Add `createUnclaimedListing` factory to `test-fixtures.ts` | next | engineer | `src/db/test-fixtures.ts` exports `createUnclaimedListing(db, overrides?)` that creates listing + companion rows with `accountId: null`, `claimStatus: "unclaimed"`, `source: "4rfv_import"`. Claim-path test imports it instead of defining inline. |
| 2 | Make `createTestListing` `accountId` param optional | later | engineer | `createTestListing(db, accountId?, overrides?)` accepts `null` or `undefined` for unclaimed listings without the `as unknown as string` cast. No test changes needed — existing calls still work. |
