# Retro: CS-WORK-035

**Date:** 2026-02-23
**Scope:** CS-WORK-035 — Subscription schema, types, and Paddle mapping (S4 foundation)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The `SubscriptionTier` misalignment between `services/types.ts` (`starter/professional/enterprise`) and `events/types.ts` / `data-and-listings.ts` (`free/standard/premium/partner`) had persisted since S0 without being caught. The DB enum was correct but the service abstraction type was stale. Only one test file referenced the old values — the blast radius was small because nothing had consumed the PaymentService tier type yet. |
| **What went well?** | Clean first-pass implementation: 11/11 AC verified, 28 tests (14 unit + 14 integration), 0 type errors, 0 regressions against existing 191 unit tests. All 15 deliverables completed. The sub-agent parallel research pattern (spec reading + codebase reading) was effective — total research phase was ~90 seconds for both agents. The WORK.md deliverable list was precise enough to work through sequentially without backtracking. |
| **Could have gone better?** | 1. `drizzle-kit push` was used instead of `drizzle-kit migrate` — acceptable for local dev but the migration file (`0006_milky_bushwacker.sql`) should be the authoritative path in CI. 2. The `onSubscriptionEvent` callback in the API route is a no-op skeleton — documented but could confuse a reader who expects the webhook to actually dispatch events. 3. Integration tests are slow (~5s each due to `resetDb()` per test). The signature verification tests don't need DB access — they could be unit tests. |
| **Keep doing** | Reading all source files before editing (caught the tier misalignment). Using Explore agents for parallel spec + codebase reads. Sequential task execution with type-check gates between tasks. Writing both unit and integration tests for the same module (mapping logic = unit, DB interactions = integration). |
| **Stop doing** | Running pure-function tests (signature verification) as integration tests. The 4 signature tests added ~22s of wall clock time for no DB benefit. |
| **Start doing** | 1. Check for type alignment gaps at the start of every S4 work item (PaymentService types vs Drizzle enums vs event payload types — three sources that must agree). 2. Add a `verifyPaddleSignature` unit test file co-located with the function, separate from the integration suite. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Stale `SubscriptionTier` in `services/types.ts` survived from S0 | Bug | Fixed during this work item. Import chain now goes events/types.ts → services/types.ts (re-export). Single source of truth. |
| 2 | Clean 11/11 AC pass on first run | Feature | All deliverables matched WORK.md spec. No iteration needed. |
| 3 | Parallel sub-agent research pattern | Feature | Two Explore agents (spec + codebase) ran concurrently, ~90s total. Established full context before first edit. |
| 4 | Signature verification tests in integration suite | Refactor | 4 pure-function tests (~22s) run against DB unnecessarily. Should be co-located unit tests. |
| 5 | `onSubscriptionEvent` skeleton in API route | Feature request | No-op callback placeholder. CS-WORK-039 wires actual dispatch. Clear but could use an inline reference to the downstream work item. |
| 6 | `PADDLE_PRICE_MAP` hardcoded with sandbox values | Feature request | Production needs env-var-driven price-to-tier mapping. Current hardcoded map only works for dev/test. CS-WORK-040 (checkout router) should address this. |
| 7 | Integration test slowness (~5s per test) | Upgrade | `resetDb()` truncates 37+ tables per test. Most webhook tests only need 3-4 tables. Not blocking but accumulates as test count grows. |

---

## 3 — Action Register

| # | Item | Priority | Owner | Definition of Done |
|---|------|----------|-------|--------------------|
| 1 | Move signature verification tests to unit test file | next | engineer | `src/domains/operations/paddle/__tests__/signature.test.ts` exists with 4 tests, integration suite drops to 10 tests, `npx vitest run` confirms both pass |
| 2 | Add inline CS-WORK-039 reference in webhook route skeleton | next | engineer | `route.ts` comment on `onSubscriptionEvent` references CS-WORK-039 by ID |
| 3 | Add env-var loading for `PADDLE_PRICE_MAP` | later | CS-WORK-040 | `PADDLE_PRICE_MAP_JSON` env var parsed at startup, falls back to hardcoded sandbox values. Tested with `createTestServices()` |
| 4 | Evaluate selective table truncation for webhook tests | later | engineer | Investigation: measure `resetDb()` with full truncation vs 4-table truncation. If >3x speedup, implement `resetTables(...names)` helper |
