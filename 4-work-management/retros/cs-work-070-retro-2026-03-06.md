---
triaged: true
status: active
---

# Retro: CS-WORK-070 — Sponsored placement selection

**Date:** 2026-03-06
**Scope:** CS-WORK-070 (10 AC). Sponsored placement selection algorithm, fairness cap, rotation, impression recording, commercial router route.

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The entire work item — algorithm, router wiring, 8 unit + 8 integration tests — completed in a single pass with 0 type errors and 0 test failures on first run. No iteration required. The 3-agent parallel pre-read (spec + code + test patterns) gave complete context before writing any code. |
| **What went well?** | Taxonomy overlap filtering was simpler than anticipated. The WORK.md suggested using the D&L `computeTaxonomyOverlap` export, but a direct `listingTaxonomyTags` JOIN was cleaner and avoided cross-domain coupling. The deduplication step (listings matching multiple tags) was a 4-line `Set`-based filter. |
| **Could have gone better?** | Initial test file had an unused `seedScenario` helper function and stale imports (`sql`, `and`, `listings`, `makeAdminSession`) that survived into the first passing run. Caught during cleanup but should have been avoided by writing only what's needed from the start. |
| **Keep doing** | 3-agent parallel pre-read before implementation. The spec agent + code agent + test pattern agent covers all context in ~20s and produces a clear mental model of what to build. |
| **Stop doing** | Writing speculative helper functions in test files before confirming they're needed. The `seedScenario` was superseded by `seedScenarioWithTax` — should have written only the one actually used. |
| **Start doing** | Nothing new — the pattern is working well. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Zero-iteration implementation (0 type errors, 0 test failures) | Feature | 3-agent pre-read + established test fixture patterns (createTestListing, seedTaxonomy, setQuality) eliminate guesswork. |
| 2 | Direct taxonomy JOIN vs cross-domain function call | Feature | Avoided importing `computeTaxonomyOverlap` from D&L. SQL WHERE on `listingTaxonomyTags` is simpler and stays within CR's query scope (reads D&L tables directly for sponsored candidate pool). |
| 3 | Unused code in initial test file | Refactor | `seedScenario` helper + 4 unused imports. Minor — caught before commit — but indicates a pattern of writing code speculatively. |
| 4 | `applyFairnessCap` couples `Math.random()` for cleanup trigger | Upgrade | 5% probabilistic cleanup is untestable deterministically. Could extract the random check as an injectable parameter for test control. Low priority — cleanup is tested directly via `cleanupOldImpressions`. |

---

## 3 — Action Register

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| 1 | Extract `cleanupProbability` parameter from `applyFairnessCap` for test determinism | later | open | Engineer | `applyFairnessCap` accepts optional `cleanupProbability` (default 0.05). Tests can pass 0 or 1 to control cleanup. Trigger: when a test needs to assert cleanup-during-fairness-cap behaviour. |
