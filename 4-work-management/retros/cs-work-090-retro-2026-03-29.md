---
triaged: true
status: complete
---

# Retro: CS-WORK-090 — Algorithm versioning and controlled rollout

**Date:** 2026-03-29
**Scope:** CS-WORK-090 (algorithm versioning, 8 AC, 7 unit + 12 integration tests)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The CRC32 lookup table implementation was trivial (~15 lines) and distributed UUIDs well across 100 buckets — no npm dependency needed. Also, the `decisionType` column being `text` (not enum) meant `"algorithm_comparison"` could be used without any migration or schema change. Both were identified during `/impl` planning but confirmed during implementation. |
| **What went well?** | Clean implementation in a single session alongside CS-WORK-088. The `evaluate.ts` stub replacement was a one-line change. The `AdminGraduationRouterDeps` intersection type automatically picked up `schedulerDb` from sibling admin router deps — no `root.ts` change needed. All 19 tests passed on second run (one case-sensitivity fix in AC-69 test). |
| **Could have gone better?** | The AC-69 test had a case-sensitivity mismatch: code produces `"Quality regression"` (capital Q) but the test searched for `"quality regression"` (lowercase). This is a test authoring error, not a production bug — caught on first run, fixed with `.toLowerCase()`. The `/impl` skill could flag case-sensitivity risk when AC text uses quoted strings that the test will search for. |
| **Keep doing** | Implementing the final work item in the same session as the penultimate item when the penultimate is test-only (088) and the final is medium effort (090). The context from 088's flow engine deep-dive carried over and accelerated 090's integration test writing. |
| **Stop doing** | N/A |
| **Start doing** | N/A |
| **Skill amendment?** | N/A — the case-sensitivity issue is a judgement pattern (read the error message), not a repeatable ceremony gap. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | CRC32 in-repo implementation — no external dep, good distribution | Feature | 15-line lookup table, deterministic, no crypto needed. Protect via AC-65/AC-66 unit tests. |
| 2 | `decisionType` text column enables free-text types without migration | Feature | `algorithm_comparison` works without schema change. Validated design decision from S0. |
| 3 | `evaluate.ts` stub replaced with one-line delegation | Feature | Clean integration point — CS-WORK-089 left a well-shaped stub that 090 replaced. |
| 4 | `AdminGraduationRouterDeps` intersection absorbed `schedulerDb` from siblings | Feature | No manual wiring needed in root.ts. TypeScript intersection types propagate deps correctly. |
| 5 | AC-69 test case-sensitivity mismatch | Bug | Test searched for lowercase `"quality regression"`, code produces uppercase `"Quality"`. Fixed with `.toLowerCase()`. |
| 6 | `scoreListingDuringRollout` accepts a `computeScore` callback — V2 algorithm not yet implemented | Feature | At V1, both versions use the same scoring formula. The callback pattern lets a future V2 algorithm plug in without changing the rollout infrastructure. No action needed — the test mocks confirm the plumbing works. |

---

## 3 — Action Register

No actionable items. All items are "Feature — protect it" (protected by CI) or the AC-69 bug (already fixed). The V2 algorithm implementation is a future feature, not tech debt — the infrastructure is complete and tested.
