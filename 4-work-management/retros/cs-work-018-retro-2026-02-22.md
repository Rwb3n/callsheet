# Retro: CS-WORK-018

**Date:** 2026-02-22
**Scope:** CS-WORK-018 — Progressive disclosure handlers and emails (6 AC, S2 onboarding)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | WORK.md title mismatch: tracker listed CS-WORK-018 as "Taxonomy suggestions" (3 AC) while the actual WORK.md says "Progressive disclosure handlers and emails" (6 AC). The decomposer must have produced the WORK.md correctly but the tracker row was stale or from an earlier iteration. Caught and corrected during implementation. |
| **What went well?** | Clean first-pass implementation. All 6 ACs verified on the first test run — no red-green-fix cycle. The existing handler registration pattern (`registerXxxHandler(registry, deps)` from `pre-claim-snapshot-cleanup.ts`) transferred directly. Both scheduling functions updated atomically with Day 14 scheduling, no existing tests broke because AC-09 test filters by `send_progressive_email` action type specifically. Template registration followed established `hasTemplate()` guard pattern. Total: 9 new integration tests, 0 regressions across 350 tests. |
| **Could have gone better?** | (1) The `isProgressiveActionComplete()` field-to-template mapping (day1=headline+bio, day3=photo+website, day7=contactEmail) was inferred from field weight ordering and general UX sense, not from an explicit spec section. S2 §7 says "skip if target action already complete" but doesn't define which template maps to which fields. The mapping is reasonable but unvalidated against design intent. (2) `InMemoryNotificationDb` duplicated a third time — tech debt trigger now met (3 files). |
| **Keep doing** | Reading full source of all touched modules before writing. The explore agent recovered the exact handler registration pattern, email preference enforcement flow, and template guard convention in one pass. No guessing. |
| **Stop doing** | Nothing specific. This was a clean, well-bounded work item. |
| **Start doing** | Extract `InMemoryNotificationDb` to `test-fixtures.ts` — the rule-of-three trigger is met with this work item. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Tracker row stale title ("Taxonomy suggestions" instead of "Progressive disclosure handlers") | Bug | Corrected during implementation. Root cause: decomposition and tracker update may have been out of sync. Check tracker accuracy on future decompositions. |
| 2 | All 6 ACs passed first run, 0 regressions | Feature | Handler pattern, template registration, preference enforcement — all well-established conventions that compose cleanly. |
| 3 | `isProgressiveActionComplete` template→field mapping not explicitly spec'd | Upgrade | Current mapping is defensible but the slice spec should ideally state which fields each template nudges. Acceptable risk — if S9 quality score factors supersede fallback fields, the mapping will be refined there. |
| 4 | `InMemoryNotificationDb` duplicated in 3 test files | Refactor | `notifications.test.ts`, `bounce.integration.test.ts`, `progressive-disclosure.integration.test.ts`. Extract to `test-fixtures.ts`. Tech debt tracker updated with trigger-met note. |
| 5 | Handler test invocation requires `as (p: Record<string, unknown>) => Promise<void>` cast | Upgrade | Type-erased `StoredActionHandler` means tests must cast the retrieved handler. A test helper like `invokeHandler(registry, action, params)` would clean this up across all handler test files. Not blocking — 3 handler test files exist. |

---

## 3 — Action Register

| # | Item | Priority | Owner | Definition of Done |
|---|------|----------|-------|--------------------|
| 1 | Extract `InMemoryNotificationDb` to `test-fixtures.ts` | next | platform | All 3 test files import from `test-fixtures.ts`. No inline `InMemoryNotificationDb` class in any test file. Tests pass. |
| 2 | Validate template→field mapping in `isProgressiveActionComplete` against S2 design intent | later | platform | S2 §7 or a design note confirms which template nudges which fields. Current mapping updated if needed. |
| 3 | Add `invokeHandler<T>(registry, action, params)` test helper | later | platform | Helper in `test-fixtures.ts` encapsulates the `registry.get()!` + cast. 3+ handler test files use it. |
