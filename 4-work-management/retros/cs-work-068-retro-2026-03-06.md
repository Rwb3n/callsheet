---
triaged: true
status: active
---

# Retro: CS-WORK-068 — Churn detection and intervention

**Date:** 2026-03-06
**Scope:** CS-WORK-068 implementation (8 AC, churn decision architecture, churn logging, event emission helpers)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The `ChurnRiskDetectedEvent` stub in `types.ts` used `riskFactors: string[]` and had no `timestamp` — plus the Ops consumer tests used a non-existent risk factor value `"usage_decline"`. Fixing upstream stubs cascaded into 5 test payload corrections. This is the same pattern seen in S7: stubs created at S0 don't match the domain spec that populates them. |
| **What went well?** | The `/impl` prep correctly identified the AC-18 partial status and all type misalignments before coding started. Implementation was clean — 0 rework cycles, all 25 tests passed on first run. The `conversion-triggers.ts` sibling provided a strong pattern reference (dependency injection, decision logging, event emission shape). |
| **Could have gone better?** | The `EngagementCounters` type is now defined in two places: `conversion-triggers.ts` and `churn-intervention.ts`. Both are identical. Should be a shared type but extracting it mid-session would touch conversion-triggers imports and tests — deferred. |
| **Keep doing** | Running `/impl` before implementation — the pre-satisfaction scan and type alignment check prevented the "discover misalignment mid-coding" failure mode. Also: fixing upstream type stubs immediately rather than casting around them. |
| **Stop doing** | Nothing identified this session. |
| **Start doing** | When narrowing event payload types (e.g., `string[]` → typed union array), grep all test files for that event type's `_brand` before committing — catches stale test payloads in one pass. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | `EngagementCounters` duplicated in `conversion-triggers.ts` and `churn-intervention.ts` | Refactor | Same 3-field type defined twice. Extract to shared location (e.g., `src/domains/commercial/types.ts` or re-export from one). |
| 2 | S0 event type stubs don't include all required fields (timestamp, typed unions) | Bug | `ChurnRiskDetectedEvent` was missing `timestamp` and had `string[]` instead of `ChurnRiskFactor[]`. Other events may have similar gaps — `QualityScoreChangedEvent` and `ErasureCompletedEvent` are still brand-only stubs. |
| 3 | Ops consumer tests used invalid `"usage_decline"` risk factor value | Bug | Tests compiled because `riskFactors` was `string[]`. Narrowing the type caught it. Fixed this session. |
| 4 | `/impl` skill correctly identified all type misalignments pre-implementation | Feature | Prevented mid-implementation surprises. The AC pre-satisfaction analysis correctly identified 0/8 pre-satisfied — accurate for this work item. |
| 5 | `CancellationReasonLiteral` in `types.ts` duplicates `CancellationReason` in `subscription/types.ts` | Refactor | Inline union in infrastructure layer to avoid circular dependency. Acceptable trade-off but creates a sync obligation if reasons are ever added. |

---

## 3 — Action Register

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| 1 | Extract shared `EngagementCounters` type to avoid duplication | later | open | commercial | Single type definition imported by both `conversion-triggers.ts` and `churn-intervention.ts`. Tests pass. |
| 2 | Audit remaining S0 event type stubs (`QualityScoreChangedEvent`, `ErasureCompletedEvent`) for missing fields | next | done | commercial | All event interfaces in `types.ts` have fields matching their interface spec definition. No brand-only stubs remain. Note: `ListingArchivedEvent` is missing 4 spec fields (`previousStatus`, `reason`, `reactivatable`, `subscriptionTier`) — deferred to CS-WORK-073 which wires the consumers that need them. |
| 3 | Add `CancellationReasonLiteral` sync check to close-out checklist | later | open | process | If `CancellationReason` union in `subscription/types.ts` is modified, `CancellationReasonLiteral` in `events/types.ts` must be updated in the same commit. Documented in MEMORY.md or CLAUDE.md. |
