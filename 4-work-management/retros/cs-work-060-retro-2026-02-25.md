---
triaged: true
status: active
---

# Retro: CS-WORK-060 — Compliance Management

**Date:** 2026-02-25
**Scope:** CS-WORK-060 (compliance register, checkComplianceHold, getDSARStatus, schedule check, self-audit, DSAR emails, admin compliance router)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | Zod v4 `z.record()` requires two arguments (`z.record(z.string(), z.unknown())`), unlike Zod v3's single-arg form. First time hitting this in the project — all prior code avoided `z.record`. |
| **What went well?** | Clean implementation cycle — 10 AC, 6 new files, 44 integration tests, zero regressions, completed in a single pass after fixing 3 type errors. Pre-checking existing patterns (billing router, SLA handler, email templates) before writing paid off — zero structural surprises. |
| **Could have gone better?** | Initial test file used non-UUID admin IDs (`"admin-compliance-router"`) which hit `decision_logs.account_id` UUID column constraint. Also used wrong `InMemoryEmailService` import path and API (`getSentEmails()` vs `getCalls()`). Three distinct test infrastructure mistakes in one file. |
| **Keep doing** | Pre-check pattern: reading billing router + SLA handler + email template patterns before writing. Copy the exact dependency injection shape, cursor pagination pattern, and email service API from existing code. |
| **Stop doing** | Guessing email service method names. Always read `src/lib/email/transport.ts` InMemoryEmailService API before writing test assertions. |
| **Start doing** | Maintain a "test infrastructure cheatsheet" in memory — the 4 most common gotchas (UUID for decision_logs.account_id, `getCalls()` not `getSentEmails()`, `resetDb()` takes no args, `invokeHandler()` not `registry.invoke()`). |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Zod v4 `z.record` two-arg requirement | Feature | Known now, will apply to all future Zod record usage. No action needed. |
| 2 | Pre-check existing patterns before writing new code | Feature | Established pattern continues to save time. |
| 3 | Non-UUID admin IDs in tests hitting UUID column constraint | Bug | `decision_logs.account_id` is `uuid()` — test admin IDs must use `makeUUID()`. Fixed in-session. |
| 4 | Wrong InMemoryEmailService import path and method names | Bug | `@/lib/email/transport` not `@/lib/email/in-memory`. `getCalls()` not `getSentEmails()`. Fixed in-session. |
| 5 | Test infrastructure API not memorised | Upgrade | Four recurring gotchas should be documented in memory to prevent repeat mistakes across sessions. |

---

## 3 — Action Register

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| 1 | Add test infrastructure cheatsheet to MEMORY.md | next | open | Engineer | `memory/MEMORY.md` or `memory/implementation-patterns.md` has a "Test Gotchas" section listing: (a) `decision_logs.account_id` is UUID, (b) `InMemoryEmailService.getCalls()` not `getSentEmails()`, (c) `resetDb()` no args, (d) `invokeHandler()` from `invoke-handler.ts`. |
