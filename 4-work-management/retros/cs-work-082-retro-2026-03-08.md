---
triaged: false
status: active
---

# Retro: CS-WORK-082

**Date:** 2026-03-08
**Scope:** CS-WORK-082 — Event consumers — CR/Ops and matrix wiring (8 AC, final S9 work item)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The `conversion_milestone` matrix going from `[]` to `[{intelligence}]` broke a commercial consumer integration test that registered a capture handler with `domain: "commercial"` — the bus matrix validation caught the mismatch. The cascade to existing tests when populating previously-empty matrix arrays was predictable but easy to miss. |
| **What went well?** | Clean implementation — 5 handlers, barrel update, matrix update, singleton wiring, 25 new tests, all passing on first full run after fixing the 2 cascade issues. Plan from `/impl` was accurate. No schema changes needed — all handlers write to existing `perception_aggregates` table with JSONB discriminated aggregateType. |
| **Could have gone better?** | The `createMockDb()` factory is duplicated between `dl-consumers.test.ts` and `cr-ops-consumers.test.ts`. Should have been extracted to shared `test-fixtures.ts` before writing the second test file. |
| **Keep doing** | Reading existing sibling handler files before writing new ones — the pattern was consistent and the new handlers matched exactly. The impl plan's type alignment section caught the `churn_analysis_log` table absence early, redirecting to `perception_aggregates`. |
| **Stop doing** | N/A |
| **Start doing** | When adding entries to previously-empty matrix arrays, grep for test files that register handlers on those event types and check if they'll fail the bus domain validation. |
| **Skill amendment?** | `/impl` Step 3 type alignment check should include: "For each matrix entry being added to a previously-empty array, grep for test files registering handlers on that event type — they may use a different domain for capture handlers and will fail bus validation." |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | `createMockDb()` duplicated in 2 intelligence consumer test files | Refactor | Same 20-line factory in `dl-consumers.test.ts` and `cr-ops-consumers.test.ts`. Extract to `test-fixtures.ts`. |
| 2 | Perception aggregate upsert pattern repeated in 4 CR/Ops handler files | Feature | Each handler has different JSONB data types, so the pattern is similar but not identical. At 4 instances + 3 in helpers.ts, approaching extraction threshold. |
| 3 | Bus matrix validation catches domain mismatches in test capture handlers | Feature | Working correctly — the validation prevented a real configuration error. |
| 4 | Empty matrix array → populated array causes cascade to tests with capture handlers | Upgrade | `/impl` skill should warn about this pattern during type alignment. |

---

## 3 — Action Register

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| 1 | Extract `createMockDb()` to shared `test-fixtures.ts` | later | open | Engineer | `createMockDb()` exported from `test-fixtures.ts`. Both `dl-consumers.test.ts` and `cr-ops-consumers.test.ts` import it instead of declaring locally. Trigger: next test file needing a chainable mock Db. |
| 2 | Extract `upsertPerceptionAggregate(db, listingId, aggregateType, period, updateFn)` helper | later | open | Engineer | Shared helper in `helpers.ts` encapsulating the select-then-insert-or-update pattern. At least 3 handler files updated. Trigger: 6th handler using the pattern. |
| 3 | Add "previously-empty matrix array" warning to `/impl` Step 3 type alignment | later | open | Skill | `/impl` Step 3 includes: "For each matrix entry added to a previously-empty array, grep test files for capture handlers on that event type — they may use a non-matching domain." |
