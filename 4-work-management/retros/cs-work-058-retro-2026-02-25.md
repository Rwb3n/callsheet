---
triaged: true
status: active
---

# Retro: CS-WORK-058 — Support triage and ticket management

**Date:** 2026-02-25
**Scope:** CS-WORK-058 implementation. 15 AC, 6 files created, 2 files modified. 34 unit + 29 integration = 63 new tests.

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The pre-check from the previous session was extremely accurate — every pre-satisfied item (schema, `adminProcedure`, `DeferredActionParamsMap`, `EmailTemplateId`, `cancelMatching`) was confirmed instantly. Zero discovery overhead. The 3-phase plan (pure → query → router) mapped cleanly to the AC structure. |
| **What went well?** | Single-pass implementation with no type errors at any checkpoint. All 15 AC verified on first run. Integration tests exercised the full triage pipeline including churn risk elevation, SLA scheduling, cancellation, email send, and decision logging — no mocks for DB-touching code. The `queries.ts` initial draft was caught and fixed before tests ran. |
| **Could have gone better?** | The `queries.ts` file was initially written with two separate queries instead of one combined `and()` filter — a copy-paste error caught during write, not during test. Should have been correct on first draft. The `create` route is 80+ lines — close to the "god route" anti-pattern. Six responsibilities (churn lookup, priority compute, SLA compute, KB deflection, insert, side effects) are inline rather than extracted. |
| **Keep doing** | Pre-check AC against existing code before implementation. The handoff notes from the prior session saved significant time. 3-phase plan (pure functions first) kept the first phase independently testable with zero DB dependency. |
| **Stop doing** | Writing placeholder file content then immediately rewriting — the initial `queries.ts` was written with wrong logic then replaced. Write once correctly or use a scratch thought first. |
| **Start doing** | Consider extracting the `create` route body into a named `triageAndCreateTicket()` orchestrator function. This would make the triage pipeline reusable (e.g., when event consumers auto-create tickets in S7 §9) and bring the route handler under the 30-line guideline. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Pre-check handoff notes eliminated discovery overhead | Feature | The MEMORY.md handoff with per-AC pre-satisfaction mapping is the fastest path into a work item. Protect this pattern. |
| 2 | 3-phase plan (pure → query → router) enabled incremental verification | Feature | Each phase passed independently before moving to the next. No "big bang" integration. |
| 3 | `create` route is ~80 lines with 6 inline responsibilities | Refactor | Candidates for extraction: churn risk lookup, triage pipeline orchestration. Would also enable reuse from event consumers (S7 §9). |
| 4 | `queries.ts` initial draft had wrong query logic | Bug | Caught before commit but wasted a write cycle. Root cause: started writing before thinking through the Drizzle query composition. |
| 5 | `sla_breach_warning` handler creates notification but doesn't send principal email | Feature request | Spec §2.7 mentions "principal email" alongside admin notification. Current handler only creates in-app notification. Email send would require `emailService` injection into the handler deps. |
| 6 | No cursor-based pagination using `id > cursor` pattern — current implementation uses limit+1 detection only | Upgrade | The `list` route detects "has more" via limit+1 but doesn't filter by cursor ID on subsequent pages. Works for page-1 only. Need to add `WHERE id > cursor` or use keyset pagination on the sort column. |

---

## 3 — Action Register

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| 1 | Extract `triageAndCreateTicket()` from `admin.support.create` route body | later | open | Engineer | Route handler calls named function. Function body ≤30 lines. Reusable from event consumers. Trigger: when S7 §9 consumer needs to auto-create tickets. |
| 2 | Add `WHERE id > cursor` to `admin.support.list` for true cursor pagination | next | open | Engineer | Passing `cursor` param to `list` returns the correct next page. Integration test verifies page-2 content differs from page-1. |
| 3 | Add principal email to `sla_breach_warning` handler | later | open | Engineer | Handler sends email via `emailService` to principal address when SLA breach approaches. Trigger: when principal email config exists (S10 or Ops §11). |
