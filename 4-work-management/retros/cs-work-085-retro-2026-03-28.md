---
triaged: true
status: active
---

# Retro: CS-WORK-085

**Date:** 2026-03-28
**Scope:** Account closure flow wiring — 8 AC, 10 deliverables (7 planned + 3 cascade). ClosureContext, buildClosureSteps, initiateAccountClosure, AccountClosedEvent amendment, SKIP_CONSTRAINTS rename, settings.ts refactor.

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | The AccountClosedEvent type amendment had a blast radius of 13 construction sites across 7 files — much larger than typical event type changes. The plan review caught this before implementation, preventing a build-breaking cascade. Without the review step, every consumer test file would have failed on first `tsc`. |
| **What went well?** | The plan → review → correct → implement cycle. The review identified 3 concrete issues (type blast radius, settings.ts refactor scope, `onConflictDoNothing` needing unique constraint) and all 3 were addressed before writing code. Zero rework during coding — every file passed on first test run except the `"essential"` enum value mistake. |
| **Could have gone better?** | Used `"essential"` as a subscription tier in the settings test fixture without checking the pgEnum definition (`"free", "standard", "premium", "partner"`). The `/impl` skill Step 3 item 5 already says to pre-read enum definitions — this was a failure to follow the existing checklist, not a skill gap. Caught immediately by the integration test runner (Postgres `22P02` error). |
| **Keep doing** | Running a structured plan review before implementation on work items that modify shared types. The type cascade analysis (grepping all `_brand: "TypeName"` construction sites) caught the entire blast radius upfront. |
| **Stop doing** | N/A |
| **Start doing** | When a work item replaces inline code in an existing router with imports from a new module, explicitly verify that the existing router's test fixtures match the new module's types (e.g., ClosureContext changed from 3 fields to 10 fields, `subscriptionsCancelled` changed from `string[]` to `number`). |
| **Skill amendment?** | N/A — the enum pre-read step already exists in `/impl` Step 3 item 5. The failure was execution, not process. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Plan review caught 13-site AccountClosedEvent blast radius before coding | Feature | The review step works — it prevented a build-breaking cascade. Keep requiring it for type-amending work items. |
| 2 | `"essential"` enum value used without checking pgEnum | Bug | Used a spec term instead of the actual DB enum value. Caught within 30 seconds by integration test. No rework beyond a single Edit. |
| 3 | Steps 3-4 as no-ops vs throwing placeholders — design decision for downstream compatibility | Feature | Chose no-ops (matching existing S5 stub behavior) over throwing (erasure pattern). This preserves the existing settings.ts integration test and allows the full flow to complete. CS-WORK-086 replaces the no-ops. |
| 4 | `pending_cancellations` table has no unique constraint for idempotent insert | Upgrade | `onConflictDoNothing()` is a no-op without a matching unique constraint. Used query-first pattern instead. A future migration could add a unique index on `(paddleSubscriptionId, reason)` to enable `onConflictDoNothing()`. |

---

## 3 — Action Register

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| 1 | Add unique index on `pending_cancellations(paddleSubscriptionId, reason)` for idempotent upsert | later | done | Platform | Migration adds unique index. `onConflictDoNothing()` usable in closure step 2 instead of query-first. Done: `uniqueIndex("pending_cancellations_sub_reason_idx")` added to schema, closure step 2 simplified to `onConflictDoNothing()`. 1,765 tests pass. |
