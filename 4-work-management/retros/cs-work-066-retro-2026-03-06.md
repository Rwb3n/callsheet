---
triaged: true
status: complete
---

# Retro: CS-WORK-066

**Date:** 2026-03-06
**Scope:** CS-WORK-066 — Commercial schema and pricing configuration (4 AC, S8 foundation)

---

## 1 — Reflection

| Prompt | Notes |
|--------|-------|
| **What surprised you?** | Integration tests failed on first run (25 files, 93 tests) but passed on immediate re-run. Likely cold-DB transient after 8-day session gap. No code change fixed it — just warm-up. |
| **What went well?** | Clean single-pass implementation. Schema matched spec exactly — no type alignment issues, no missing imports, no FK surprises. Existing `commercial.ts` already had the right imports pattern from `grace_periods`. Migration generated cleanly with all 5 indexes. |
| **Could have gone better?** | Two PRICING modules now coexist: S4's array-of-objects in `subscription/pricing.ts` (display-oriented, includes `targetPersona`) and S8's Record in `pricing-config.ts` (computation-oriented). Not a bug — they serve different consumers — but the naming overlap could confuse future work items. |
| **Keep doing** | Pre-reading existing files (schema, pricing, test-utils) before writing. The close-out checklist for test-utils table lists caught immediately. Running custom-sql after push. |
| **Stop doing** | Nothing flagged. |
| **Start doing** | For foundation work items that add tables but no routes/consumers, verify the tables exist in DB by running a quick SQL check (e.g., `\dt commercial_state`) rather than relying solely on integration test pass-through. The integration tests don't directly exercise the new tables — they only confirm the truncation list doesn't error. |

---

## 2 — Classification

| # | Item | Bucket | Detail |
|---|------|--------|--------|
| 1 | Transient integration test failure on cold DB | Bug | First run after 8-day gap: 93 failures. Second run: 0. No code change. Likely Postgres connection pool warm-up or Docker container startup latency. |
| 2 | Single-pass implementation with no type errors | Feature | Schema, PRICING, migration, test-utils update, unit tests — all correct first time. Spec-to-code alignment strong. |
| 3 | Dual PRICING modules (S4 array vs S8 Record) | Refactor | `subscription/pricing.ts` (TierPricing[] with targetPersona) and `pricing-config.ts` (Record<SubscriptionTier, {annual, monthly}>). Both valid, different consumers. Consider consolidating or clearly documenting the split. |
| 4 | No direct DB verification step for schema-only work items | Feature request | Foundation items that add tables but no routes don't exercise the new tables through integration tests. A quick DB existence check would catch migration issues earlier. |

---

## 3 — Action Register

| # | Item | Priority | Status | Owner | Definition of Done |
|---|------|----------|--------|-------|--------------------|
| 1 | Document dual PRICING module rationale in a code comment | now | done | Engineer | `pricing-config.ts` has a comment referencing `subscription/pricing.ts` and explaining the split (computation vs display). |
| 2 | Add DB table existence smoke check to foundation work item close-out | now | done | Engineer | Close-out checklist added to MEMORY.md: "After `drizzle-kit push` for schema-only work items, run `SELECT 1 FROM {new_table} LIMIT 0`." |
